<% content_for :title, "Edit Portofolio" %>
<div class="container mt-5 mb-5">
  <h1>Edit Portofolio</h1>
  <%= render "form", portofolio: @portofolio %>
  <div>
    <%= link_to "Show this portofolio", @portofolio, {class: "btn btn-info me-2"} %>
    <%= link_to "Back to list portofolio", "/portofolios?id_user=#{session[:user_id]}", {class: "btn btn-secondary"} %>
  </div>
</div>
