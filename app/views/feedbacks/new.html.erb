<% content_for :title, "New Feedback" %>
<div class="container mt-5 mb-5">
  <h1 class="title-cus">Add New Feedback</h1>
  <%= render "form", feedback: @feedback %>
  <div>
    <%= link_to "Back to feedback", "/feedbacks?id_user=#{session[:user_id]}", {class: 'btn btn-secondary'} %>
  </div>
</div>
