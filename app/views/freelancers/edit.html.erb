<% content_for :title, "Edit Profile" %>
<div class="container mt-5 mb-5">
  <h1>Edit Profile</h1>
  <%= render "form", freelancer: @freelancer %>
  <div class="mt-5">
    <%= link_to "Back To Profile", current_user, {class: "btn btn-secondary"} %>
  </div>
</div>
